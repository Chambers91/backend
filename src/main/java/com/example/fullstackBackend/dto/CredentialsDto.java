package com.example.fullstackBackend.dto;

//used for record of each field only
//records are immutable(unable to be changed over time)
public class CredentialsDto (String login, char[] password) {
}
